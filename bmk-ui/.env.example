VITE_APP_DOMAIN=localhost
VITE_TENANT_HOST_SUFFIX=
VITE_PLATFORM_HOSTNAME=localhost
# Leave empty in Docker / Vite so the browser uses same-origin /api (proxy).
VITE_API_BASE_URL=
# local | test | staging | production — shown as a banner on the platform home page
VITE_APP_ENV=local
